[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
# One event loop for the whole session, so the shared asyncpg pool created in
# conftest stays valid across tests instead of being bound to a closed loop.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
addopts = -q
